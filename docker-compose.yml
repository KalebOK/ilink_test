version: '3'
services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: admin
    ports:
      - 27017:27017
    volumes:
      - ./postgres:/data/db
  
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
